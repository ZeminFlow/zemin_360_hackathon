# React Bits integration targets

Official React Bits source was not available locally during this pass. No fabricated React Bits implementation is used.

Copy these official React/Tailwind components from React Bits when their source is available:

1. **Backgrounds → Dark Veil** → replace `DarkVeil.tsx` and include the dependency documented by React Bits if ambient motion is added to Home later.
2. **Cards → Spotlight Card** → add only if the Discover intelligence profiles benefit from it after static visual approval.
3. **Animations → Animated List** → add only if the Network feed benefits from it after static visual approval.

The former Pill Nav direction was intentionally removed. Preserve the current Swiss-editorial composition when adding any official source.
