/**
 * Integration target for the official React Bits "Pill Nav" source.
 *
 * The official component is not available locally, so this file intentionally
 * contains no imitation. Copy the React/Tailwind Pill Nav source from React
 * Bits into this file, then replace PillNavFallback in AppLayout.
 */
export {}
